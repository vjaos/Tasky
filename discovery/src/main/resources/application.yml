spring:
  application:
    name: discovery

server:
  port: 8087

eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://discovery:8087/eureka
